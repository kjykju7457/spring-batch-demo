spring:
  datasource:
    url: jdbc:mysql://localhost:3306/dev
    username: root
    password: 1111
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create
  batch:
    job:
      enabled: false